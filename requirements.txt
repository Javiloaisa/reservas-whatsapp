# --- Core backend ---
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
alembic>=1.13
pydantic>=2.6
pydantic-settings>=2.2

# --- Integraciones ---
anthropic>=0.40
httpx>=0.27

# --- Sesiones del panel (cookie firmada de Starlette SessionMiddleware) ---
itsdangerous>=2.1

# --- UI del panel (plantillas server-side) ---
jinja2>=3.1
python-multipart>=0.0.9   # formularios del panel (form-urlencoded)

# --- Auth del panel (seed de admin; se usa de lleno en fases posteriores) ---
# bcrypt fijado <4.1: passlib 1.7.4 es incompatible con la API de bcrypt 4.1+.
passlib>=1.7
bcrypt>=4.0,<4.1

# --- Zona horaria en Windows (zoneinfo necesita la base de datos tzdata) ---
tzdata>=2024.1

# --- Google Calendar (solo se importan/usan cuando hay credenciales en .env) ---
google-api-python-client>=2.120
google-auth>=2.29

# --- PostgreSQL en produccion (en desarrollo se usa SQLite, sin driver extra) ---
# psycopg[binary]>=3.1   # descomentar al desplegar contra PostgreSQL

# --- Desarrollo / pruebas (no se instalan en produccion) ---
pytest>=8.0
